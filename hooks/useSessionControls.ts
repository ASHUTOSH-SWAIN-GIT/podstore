// Re-export the refactored useSessionControls hook
export { useSessionControls } from "./session";

// Export types for external use
export type { SessionControls, SessionState, SessionActions } from "./session/types";
 
